---
title: "Code zur Masterarbeit"
author: "Xuan Son Le (4669361)"
date: "Abgabedatum: 10/10/2018"
output: pdf_document: keep_tex: true
editor_options: 
  chunk_output_type: console
---

```{r Setup, include=FALSE}
# Libraries importieren
knitr::opts_chunk$set(echo = TRUE)  
library(dplyr)
library(ggplot2)
library(xtable)
library(Hmisc)
library(params)
library(gridExtra)
options(xtable.floating = FALSE)
options(xtable.timestamp = "")
#options(scipen = 999)
```

```{r Daten importieren, warning=FALSE}
# CSV-Dateien einlesen
censusDF <- read.csv('c.csv')[,-1] #Spalte 1 enthält nur Zeilennummer und wird entfernt
surveyDF <- read.csv('s.csv')[,-1] #Spalte 1 enthält nur Zeilennummer und wird entfernt

# Nur gemeinsame erklärende Variablen werden behalten
censusDF <- censusDF[,colnames(censusDF) %in% colnames(surveyDF)]
surveyDF <- data.frame("cuadrantes" = surveyDF[,"cuadrantes"],
                       surveyDF[,colnames(surveyDF) %in% colnames(censusDF)])

```

```{r Datenverständnis & -manipulation, warning = FALSE}
# Deskriptive Statistik
#describe(surveyDF)
#describe(censusDF)

# Anteil an fehlenden Werten bestimmen und in einer Tabelle zusammenfassen
missingSurvey <- data.frame("Variable" = colnames(surveyDF), "MissingSurvey" = t(data.frame(lapply(surveyDF, function(x) round(100*sum(is.na(x))/length(x),1)))), row.names = NULL)
missingCensus <- data.frame("Variable" = colnames(censusDF), "MissingCensus" = t(data.frame(lapply(censusDF, function(x) round(100*sum(is.na(x))/length(x),1)))), row.names = NULL)
missingRmVar <- left_join(missingSurvey, missingCensus, by = 'Variable')
missingAll <- left_join(missingSurvey, missingCensus, by = 'Variable')
missingRmVar <- missingRmVar[which(missingRmVar[,2] > 10 & missingRmVar[,3] > 10),]
row.names(missingRmVar) <- c(1:nrow(missingRmVar))

rm(missingSurvey)
rm(missingCensus)

# Latex-Tabelle-Formatierung 
#xtable(missingRmVar) 

# Variablen mit zu vielen fehlenden Werten in beiden Datensätzen werden entfernt
censusDF <- censusDF[, !(colnames(censusDF) %in% missingRmVar[1:5,1])]
surveyDF <- surveyDF[, !(colnames(surveyDF) %in% missingRmVar[1:5,1])]

# Hilfsvariablen werden entfernt
hilfsvariablen <- c('est_dis', 'factor', 'mun', 'upm')
surveyDF <- surveyDF[ , !(names(surveyDF)  %in% hilfsvariablen)]
censusDF <- censusDF[ , !(names(censusDF)  %in% hilfsvariablen)]
rm(hilfsvariablen)

# Variablen mit knapp Zero-Varianz werden entfernt
library(caret)
nearZeroVar(surveyDF, names = TRUE)
nearZeroVar(censusDF, names = TRUE)

censusDF <- censusDF[, -which(colnames(censusDF) %in% c('ent', 'habl_ind'))]
surveyDF <- surveyDF[, -which(colnames(surveyDF) %in% c('ent', 'habl_ind'))]

# Transformation numerischer in kategorialer Variablen. Ausgenommen sind edad und anos_escolaridad, da sie metrisch skaliert sind.
surveyDF$cuadrantes <- factor(surveyDF$cuadrantes, levels = c(1,2,3,4,5), 
                                 labels = c('extrem','gemildert',
                                            'anfällig aufgrund Einkommen', 
                                            'anfällig aufgrund Sozialmangel', 'keine'))

surveyDF$asis_esc <- surveyDF$asis_esc - 1 #Damit gleiche Werte mit Census Datei
censusDF$sexo <- censusDF$sexo - 1 #Damit gleiche Werte mit Survey Datei

names(surveyDF)[6] <- 'anos_escolaridad' #Umbenennung für richtige Zeichencodierung
names(censusDF)[5] <- 'anos_escolaridad' #Umbenennung für richtige Zeichencodierung

listDF <- c("surveyDF", "censusDF")

for (i in c(1,2)) {
    
    df <- list(surveyDF, censusDF)[[i]]
    
    df$parentesco <- factor(df$parentesco)
    df$sexo <- factor(df$sexo, levels = c(0,1), labels = c('Weiblich','Männlich'))
    #df$habl_ind <- factor(df$habl_ind, levels = c(0,1), labels = c('Nein','Ja'))
    df$asis_esc <- factor(df$asis_esc, levels = c(0,1), labels = c('Nein','Ja'))
    df$pea <- factor(df$pea)
    df$c_ocupa <- factor(df$c_ocupa)
    df$sector_act <- factor(df$sector_act)
    df$jub <- factor(df$jub, levels = c(0,1), labels = c('Nein','Ja'))
    df$dhind_cy <- factor(df$dhind_cy, levels = c(0,1), labels = c('Nein','Ja'))
    df$dhind_parent <- factor(df$dhind_parent, levels = c(0,1), labels = c('Nein','Ja'))
    df$ic_rezedu <- factor(df$ic_rezedu, levels = c(0,1), labels = c('Nein','Ja'))
    df$ic_asalud <- factor(df$ic_asalud, levels = c(0,1), labels = c('Nein','Ja'))
    df$ic_cv <- factor(df$ic_cv, levels = c(0,1), labels = c('Nein','Ja'))
    df$ic_sbv <- factor(df$ic_sbv, levels = c(0,1), labels = c('Nein','Ja'))
    df$rururb <- factor(df$rururb, levels = c(0,1), labels = c('Städtisch','Ländlich'))
    df$tam_loc <- factor(df$tam_loc, levels = c(1,2,3,4), labels = c('mehr als 100.000','15.000 bis 99.999', '2.500 bis 14.999', 'weniger als 2.500'))
    df$idD <- factor(df$idD)
    
    assign(listDF[i], df , envir = .GlobalEnv)
}

rm(df)
rm(listDF)
rm(i)
```

```{r Diagramme, warning = FALSE}
# Verteilung Armutskategorie 
abb61 <- ggplot(data = surveyDF, aes(x = cuadrantes, fill = cuadrantes)) +
            geom_bar(width = 0.75) +
            theme_light() +
            labs(y = "Anzahl der Beobachtung",
                 x = "Armutskategorie") +
            scale_fill_discrete(guide = FALSE) +
            scale_x_discrete(labels = c("extreme\nArmut", "gemilderte\nArmut", 
                                        "anfällige Armut\naufgrund\nniedrigem\nEinkommen",
                                        "anfällige Armut\naufgrund\nSozialmangel", 
                                        "keine\nArmut")) +
            theme(#plot.title = element_text(size = 28, face = "bold"),
                  axis.line.x = element_line(color = "black", size = 0.5),
                  axis.line.y = element_line(color = "black", size = 0.5),
                  axis.text.x = element_text(size = 20, margin = margin(t = 5, r = 0, b = 10, l = 0)),
                  axis.text.y = element_text(size = 20, margin = margin(t = 0, r = 0, b = 0, l = 10)),
                  #axis.title.x = element_text(size = 24, face = "bold"),
                  axis.title.x = element_blank(),
                  axis.title.y = element_text(size = 24, face = "bold"),
                  axis.ticks.length = unit(.25, "cm"),
                  axis.ticks = element_line(colour = "black"))

ggsave(plot = abb61, filename = "Abbildung_6_1.pdf", width = 297, height = 210, units = "mm")

# Armutskategorie nach Geschlecht
abb62 <- ggplot(data = surveyDF, aes(x = cuadrantes, fill = sexo)) +
            geom_bar(position = 'dodge', width = 0.75) +
            theme_light() +
            labs(#title = "C.2 Armutskategorie nach Geschlecht",
                 y = "Anzahl der Beobachtung",
                 x = " ",
                 fill = "Geschlecht") +
            scale_x_discrete("Armutskategorie",
                             labels = c("extreme\nArmut", "gemilderte\nArmut", 
                                        "anfällige Armut\naufgrund\nniedrigem\nEinkommen",
                                        "anfällige Armut\naufgrund\nSozialmangel", 
                                        "keine\nArmut")) +
            theme(#plot.title = element_text(size = 28, face = "bold"),
                  axis.line.x = element_line(color = "black", size = 0.5),
                  axis.line.y = element_line(color = "black", size = 0.5),
                  axis.text.x = element_text(size = 20, margin = margin(t = 5, r = 0, b = 10, l = 0)),
                  axis.text.y = element_text(size = 20, margin = margin(t = 0, r = 0, b = 0, l = 10)),
                  #axis.title.x = element_text(size = 24, face = "bold"),
                  axis.title.x = element_blank(),
                  axis.title.y = element_text(size = 24, face = "bold"),
                  axis.ticks.length = unit(.25, "cm"),
                  axis.ticks = element_line(colour = "black"),
                  legend.text = element_text(size = 18),
                  legend.title = element_text(size = 20, face = "bold"),
                  legend.position = c(0.9,0.9),
                  legend.background = element_blank(),
                  legend.box.background = element_rect(colour = "black"))

ggsave(plot = abb62, filename = "Abbildung_6_2.pdf", width = 297, height = 210, units = "mm")


# surveyDF <- surveyDF %>% mutate(ageGroup = case_when(
#         edad < 6 ~ "< 6",
#         edad < 18 ~ "10-18",
#         edad < 65 ~ "18-65",
#         TRUE ~ "> 65"
#     )
# )

# surveyDF <- surveyDF %>% mutate(eduLevel = case_when(
#         anos_escolaridad == 0 ~ "Kein Schulbesuch",
#         anos_escolaridad <= 6 ~ "Grundschule",
#         anos_escolaridad <= 9 ~ "Mittelschule",
#         anos_escolaridad <= 12 ~ "Oberschule", 
#         TRUE ~ "Studium"
#     )
# )
# 
# surveyDF$eduLevel <- factor(surveyDF$eduLevel, levels = c("Kein Schulbesuch", "Grundschule", "Mittelschule", "Oberschule","Studium"))

# Armutskategorie nach Alter
abb63 <- ggplot(data = surveyDF, aes(x = edad, fill = cuadrantes)) +
            geom_histogram(position = "fill", bins = 30) +
            geom_vline(xintercept = mean(surveyDF$edad), linetype = "dashed", size = 1) +
            theme_light() +
            labs(#title = "C.3 Armutskategorie nach Alter",
                 y = "Anteil der Beobachtungen",
                 x = "Alter",
                 fill = "Armutskategorie") +
            scale_x_continuous(breaks = seq(0, 100, by = 10), expand = c(0,0)) +
            scale_y_continuous(expand = c(0,0)) +
            scale_fill_discrete("Armutskategorie",
                                breaks = c("1", "2", "3", "4", "5"), 
                                labels = c("extreme Armut\t", "gemilderte Armut\t", 
                                        "anfällige Armut aufgrund niedrigem Einkommen",
                                        "anfällige Armut aufgrund Sozialmangel", 
                                        "keine Armut")) +
            theme(#plot.title = element_text(size = 28, face = "bold"),
                  axis.line.x = element_line(color = "black", size = 0.5),
                  axis.line.y = element_line(color = "black", size = 0.5),
                  axis.text.x = element_text(size = 20, margin = margin(t = 5, r = 0, b = 10, l = 0)),
                  axis.text.y = element_text(size = 20, margin = margin(t = 0, r = 0, b = 0, l = 10)),
                  axis.title.x = element_text(size = 24, face = "bold"),
                  #axis.title.x = element_blank(),
                  axis.title.y = element_text(size = 24, face = "bold"),
                  axis.ticks.length = unit(.25, "cm"),
                  axis.ticks = element_line(colour = "black"),
                  legend.title = element_blank(),
                  legend.margin = margin(c(0,10,0,0)),
                  legend.text = element_text(size = 18),
                  legend.position = "bottom",
                  legend.background = element_blank(),
                  legend.box.background = element_rect(colour = "black"),
                  legend.box.margin = margin(c(5,0,5,0)),
                  legend.key.width = unit(1.5,"line"),
                  legend.key.height = unit(2,"line")) +
            guides(fill = guide_legend(nrow = 2))

ggsave(plot = abb63, filename = "Abbildung_6_3.pdf", width = 297, height = 210, units = "mm")

# Armutskategorie nach Bildungsjahre
abb64 <- ggplot(data = surveyDF, aes(x = anos_escolaridad, fill = cuadrantes)) +
            geom_histogram(stat = 'count', position = 'fill') +
            theme_light() +
            labs(#title = "C.4 Armutskategorie nach Bildungsdauer",
                 y = "Anteil der Beobachtungen",
                 x = "Bildungsjahre",
                 fill = "Armutskategorie") + 
            #scale_x_discrete(limits = c('Kein Schulbesuch','Grundschule', 'Mittelschule', 'Oberschule', 'Studium')) +
            scale_x_continuous(breaks = seq(min(surveyDF$anos_escolaridad, na.rm = TRUE), 
                                            max(surveyDF$anos_escolaridad, na.rm = TRUE), by = 1)) +
            scale_fill_discrete("Armutskategorie",
                                breaks = c("1", "2", "3", "4", "5"), 
                                labels = c("extreme Armut\t", "gemilderte Armut\t", 
                                        "anfällige Armut aufgrund niedrigem Einkommen",
                                        "anfällige Armut aufgrund Sozialmangel", 
                                        "keine Armut")) +
            theme(#plot.title = element_text(size = 28, face = "bold"),
                  axis.line.x = element_line(color = "black", size = 0.5),
                  axis.line.y = element_line(color = "black", size = 0.5),
                  axis.text.x = element_text(size = 20, margin = margin(t = 5, r = 0, b = 10, l = 0)),
                  axis.text.y = element_text(size = 20, margin = margin(t = 0, r = 0, b = 0, l = 10)),
                  axis.title.x = element_text(size = 24, face = "bold"),
                  #axis.title.x = element_blank(),
                  axis.title.y = element_text(size = 24, face = "bold"),
                  axis.ticks.length = unit(.25, "cm"),
                  axis.ticks = element_line(colour = "black"),
                  legend.title = element_blank(),
                  legend.margin = margin(c(0,10,0,0)),
                  legend.text = element_text(size = 18),
                  legend.position = "bottom",
                  legend.background = element_blank(),
                  legend.box.background = element_rect(colour = "black"),
                  legend.box.margin = margin(c(5,0,5,0)),
                  legend.key.width = unit(1.5,"line"),
                  legend.key.height = unit(2,"line"),
                  strip.text = element_text(colour = "black", size = 16)) +
            guides(fill = guide_legend(nrow = 2)) +
            facet_grid(ic_rezedu ~ ., labeller = as_labeller(c('Nein' = 'Keine Verzögerung',
                                                               'Ja' = 'Mit Verzögerung'))) 

ggsave(plot = abb64, filename = "Abbildung_6_4.pdf", width = 297, height = 210, units = "mm")

# Armutskategorie nach Wohnort
abb65a <- ggplot(data = surveyDF, aes(x = rururb, fill = cuadrantes)) +
            geom_histogram(stat = 'count', position = 'fill') +
            theme_light() +
            labs(#title = "C.5 Armutskategorie nach Wohnort",
                 y = "Anteil der Beobachtung je Wohnort",
                 x = " ") +
            scale_fill_discrete(labels = c("extreme Armut\t", "gemilderte Armut\t", 
                                        "anfällige Armut aufgrund niedrigem Einkommen",
                                        "anfällige Armut aufgrund Sozialmangel", 
                                        "keine Armut")) +
            # scale_x_discrete(breaks = c("0", "1"), 
            #                  labels = c("städtisch", "ländlich"),
            #                  limits = c("1", "0")) +
            scale_y_continuous(labels = scales::percent_format()) +
            theme(#plot.title = element_text(size = 28, face = "bold"),
                  axis.line.x = element_line(color = "black", size = 0.5),
                  axis.line.y = element_line(color = "black", size = 0.5),
                  axis.text.x = element_text(size = 20, margin = margin(t = 5, r = 0, b = 10, l = 0)),
                  axis.text.y = element_text(size = 20, margin = margin(t = 0, r = 0, b = 0, l = 10)),
                  #axis.title.x = element_text(size = 24, face = "bold"),
                  axis.ticks.length = unit(.25, "cm"),
                  axis.ticks = element_line(colour = "black"),
                  axis.title.x = element_blank(),
                  axis.title.y = element_text(size = 24, face = "bold"),
                  legend.title = element_blank(),
                  legend.margin = margin(c(0,10,0,0)),
                  legend.text = element_text(size = 18),
                  legend.position = "bottom",
                  legend.background = element_blank(),
                  legend.box.background = element_rect(colour = "black"),
                  legend.box.margin = margin(c(5,0,5,0)),
                  legend.key.width = unit(1.5,"line"),
                  legend.key.height = unit(2,"line"),
                  strip.text = element_text(colour = "black", size = 16)) +
            guides(fill = guide_legend(nrow = 2)) 

ggsave(plot = abb65a, filename = "Abbildung_6_5a.pdf", width = 297, height = 210, units = "mm")

# Armutskategorie nach Wohnortgröße
abb65b <- ggplot(data = surveyDF, aes(x = tam_loc, fill = cuadrantes)) +
            geom_histogram(stat = 'count', position = 'fill') +
            theme_light() +
            labs(#title = "C.6 Armutskategorie nach Einwohnerzahl des Wohnorts",
                 y = "Anteil der Beobachtung je Wohnortgröße",
                 x = " ") +
            scale_fill_discrete(labels = c("extreme Armut\t", "gemilderte Armut\t", 
                                        "anfällige Armut aufgrund niedrigem Einkommen",
                                        "anfällige Armut aufgrund Sozialmangel", 
                                        "keine Armut")) +
            scale_x_discrete(limits = c("weniger als 2.500", "2.500 bis 14.999",
                                           "15.000 bis 99.999", "mehr als 100.000")) +
            scale_y_continuous(labels = scales::percent_format()) +
            theme(#plot.title = element_text(size = 28, face = "bold"),
                  axis.line.x = element_line(color = "black", size = 0.5),
                  axis.line.y = element_line(color = "black", size = 0.5),
                  axis.text.x = element_text(size = 20, margin = margin(t = 5, r = 0, b = 10, l = 0)),
                  axis.text.y = element_text(size = 20, margin = margin(t = 0, r = 0, b = 0, l = 10)),
                  #axis.title.x = element_text(size = 24, face = "bold"),
                  axis.title.x = element_blank(),
                  axis.title.y = element_text(size = 24, face = "bold"),
                  axis.ticks.length = unit(.25, "cm"),
                  axis.ticks = element_line(colour = "black"),
                  legend.title = element_blank(),
                  legend.margin = margin(c(0,10,0,0)),
                  legend.text = element_text(size = 18),
                  legend.position = "bottom",
                  legend.background = element_blank(),
                  legend.box.background = element_rect(colour = "black"),
                  legend.box.margin = margin(c(5,0,5,0)),
                  legend.key.width = unit(1.5,"line"),
                  legend.key.height = unit(2,"line"),
                  strip.text = element_text(colour = "black", size = 16)) +
            guides(fill = guide_legend(nrow = 2)) +
            facet_grid(rururb ~ .)

ggsave(plot = abb65b, filename = "Abbildung_6_5b.pdf", width = 297, height = 210, units = "mm")

# Verteilung parentsco
abb66a <- ggplot(data = surveyDF, aes(x = parentesco, fill = cuadrantes)) +
            geom_bar(position = 'stack', width = 0.75) +
            theme_light() +
            labs(y = "Anzahl der Beobachtung") +
            scale_fill_discrete("Armutskategorie",
                                labels = c("extreme Armut\t", "gemilderte Armut\t", 
                                        "anfällige Armut aufgrund niedrigem Einkommen",
                                        "anfällige Armut aufgrund Sozialmangel", 
                                        "keine Armut")) +
            theme(#plot.title = element_text(size = 28, face = "bold"),
                  axis.line.x = element_line(color = "black", size = 0.5),
                  axis.line.y = element_line(color = "black", size = 0.5),
                  axis.text.x = element_text(size = 20, margin = margin(t = 5, r = 0, b = 10, l = 0)),
                  axis.text.y = element_text(size = 20, margin = margin(t = 0, r = 0, b = 0, l = 10)),
                  #axis.title.x = element_text(size = 24, face = "bold"),
                  axis.title.x = element_blank(),
                  axis.title.y = element_text(size = 24, face = "bold"),
                  axis.ticks.length = unit(.25, "cm"),
                  axis.ticks = element_line(colour = "black"),
                  legend.title = element_blank(),
                  legend.margin = margin(c(0,10,0,0)),
                  legend.text = element_text(size = 18),
                  legend.position = "bottom",
                  legend.background = element_blank(),
                  legend.box.background = element_rect(colour = "black"),
                  legend.box.margin = margin(c(5,0,5,0)),
                  legend.key.width = unit(1.5,"line"),
                  legend.key.height = unit(2,"line")) +
            guides(fill = guide_legend(nrow = 2)) 

ggsave(plot = abb66a, filename = "Abbildung_6_6a.pdf", width = 297, height = 210, units = "mm")

surveyDF <- surveyDF %>% mutate(parentesco = case_when(
        parentesco %in% c(101,201,301) ~ as.numeric(as.character(surveyDF$parentesco)),
        TRUE ~ as.numeric(999)
    )
)
surveyDF$parentesco <- factor(surveyDF$parentesco)

abb66b <- ggplot(data = surveyDF, aes(x = parentesco, fill = cuadrantes)) +
            geom_bar(position = 'stack', width = 0.75) +
            theme_light() +
            labs(y = "Anzahl der Beobachtung") +
            scale_fill_discrete("Armutskategorie",
                                labels = c("extreme Armut\t", "gemilderte Armut\t", 
                                        "anfällige Armut aufgrund niedrigem Einkommen",
                                        "anfällige Armut aufgrund Sozialmangel", 
                                        "keine Armut")) +
            theme(#plot.title = element_text(size = 28, face = "bold"),
                  axis.line.x = element_line(color = "black", size = 0.5),
                  axis.line.y = element_line(color = "black", size = 0.5),
                  axis.text.x = element_text(size = 20, margin = margin(t = 5, r = 0, b = 10, l = 0)),
                  axis.text.y = element_text(size = 20, margin = margin(t = 0, r = 0, b = 0, l = 10)),
                  #axis.title.x = element_text(size = 24, face = "bold"),
                  axis.title.x = element_blank(),
                  axis.title.y = element_text(size = 24, face = "bold"),
                  axis.ticks.length = unit(.25, "cm"),
                  axis.ticks = element_line(colour = "black"),
                  legend.title = element_blank(),
                  legend.margin = margin(c(0,10,0,0)),
                  legend.text = element_text(size = 18),
                  legend.position = "bottom",
                  legend.background = element_blank(),
                  legend.box.background = element_rect(colour = "black"),
                  legend.box.margin = margin(c(5,0,5,0)),
                  legend.key.width = unit(1.5,"line"),
                  legend.key.height = unit(2,"line")) +
            guides(fill = guide_legend(nrow = 2)) 

ggsave(plot = abb66b, filename = "Abbildung_6_6b.pdf", width = 297, height = 210, units = "mm")

c9 <- ggplot(data = surveyDF, aes(x = c_ocupa, fill = cuadrantes)) +
            geom_bar(position = 'stack', width = 0.75) +
            theme_light() +
            labs(y = "Anzahl der Beobachtung") +
            scale_fill_discrete("Armutskategorie",
                                labels = c("extreme Armut\t", "gemilderte Armut\t", 
                                        "anfällige Armut aufgrund niedrigem Einkommen",
                                        "anfällige Armut aufgrund Sozialmangel", 
                                        "keine Armut")) +
            theme(#plot.title = element_text(size = 28, face = "bold"),
                  axis.line.x = element_line(color = "black", size = 0.5),
                  axis.line.y = element_line(color = "black", size = 0.5),
                  axis.text.x = element_text(size = 20, margin = margin(t = 5, r = 0, b = 10, l = 0)),
                  axis.text.y = element_text(size = 20, margin = margin(t = 0, r = 0, b = 0, l = 10)),
                  #axis.title.x = element_text(size = 24, face = "bold"),
                  axis.title.x = element_blank(),
                  axis.title.y = element_text(size = 24, face = "bold"),
                  axis.ticks.length = unit(.25, "cm"),
                  axis.ticks = element_line(colour = "black"),
                  legend.title = element_blank(),
                  legend.margin = margin(c(0,10,0,0)),
                  legend.text = element_text(size = 18),
                  legend.position = "bottom",
                  legend.background = element_blank(),
                  legend.box.background = element_rect(colour = "black"),
                  legend.box.margin = margin(c(5,0,5,0)),
                  legend.key.width = unit(1.5,"line"),
                  legend.key.height = unit(2,"line")) +
            guides(fill = guide_legend(nrow = 2)) 

c9

ggsave(plot = c9, filename = "C9.pdf", width = 297, height = 210, units = "mm")
```

```{r Behandlung fehlender Werte, warning = FALSE}
# Fehlende Werte asis_esc & habl_ind 
## (surveyDF: Modus je Armutskategorie; censusDF: Modus)
Mode2 <- function(x) {tab <- table(x); names(tab)[which.max(tab)]} #Quelle: https://stat.ethz.ch/pipermail/r-help/2010-March/231975.html 
aggregate(surveyDF[c('asis_esc')],
          by = list(surveyDF$cuadrantes),
          FUN = Mode2)
rm(Mode2)

surveyDF$asis_esc[which(is.na(surveyDF$asis_esc))] <- 'Ja'
#surveyDF$habl_ind[which(is.na(surveyDF$habl_ind))] <- 'Nein'

censusDF$asis_esc[which(is.na(surveyDF$asis_esc))] <- names(which.max(table(censusDF['asis_esc'])))
#censusDF$habl_ind[which(is.na(surveyDF$habl_ind))] <- names(which.max(table(censusDF['habl_ind'])))

# Fehlende Werte anos_escolaridad 
# (surveyDF: Median je Armutskategorie; censusDF: Median)
anosCuad <- aggregate(surveyDF['anos_escolaridad'],
          by = list(surveyDF$cuadrantes),
          FUN = median,
          na.rm = TRUE,
          na.action = na.pass)
for (i in c('extrem','gemildert','anfällig aufgrund Einkommen', 'anfällig aufgrund Sozialmangel', 'keine')) {
    
    surveyDF$anos_escolaridad[which(is.na(surveyDF$anos_escolaridad) &
                           surveyDF$cuadrantes == i)] <- anosCuad[which(anosCuad == i),2]
    
}

rm(i)
rm(anosCuad)

censusDF$anos_escolaridad[which(is.na(surveyDF$anos_escolaridad))] <- median(censusDF$anos_escolaridad, na.rm = TRUE)

# Restliche NAs werden aus censusDF entfernt
censusDF <- na.omit(censusDF)

# Kontrollieren, ob alle fehlenden Werte schon bereinigt wurden
all(!is.na(surveyDF))
all(!is.na(censusDF))
```

```{r Vorbereitung für Modellierung}

# Datensatz in Trainings- und Testdatendatz splitten
library(caTools)
set.seed(123)
split = sample.split(surveyDF$cuadrantes, SplitRatio = 0.75)
surveyTrainDF = subset(surveyDF, split == TRUE)
surveyTestDF = subset(surveyDF, split == FALSE)
rm(split)

# Vorbereitung k-fache Kreuzvalidierung
library(caret)
set.seed(123)
folds <- createFolds(surveyDF$cuadrantes, k = 10, list = TRUE, returnTrain = FALSE)

```

```{r Modellierung Multimomiale logistische Regression}
library(nnet)

# MLR k-fache Kreuzvalidierung
#MLR_Accuracy <- data.frame(Fold = integer(), Accuracy = integer())
MLR_RmVar <- data.frame(Fold = integer(), rmVar = integer())
#MLR_ConfMat <- list()

for (i in c(1:10)) {
    
    # k-1 Folds für Training und 1 für Valid
    validID <- i
    trainID <- c(1:10)[which(!(c(1:10) == i))]
    validDF <- surveyTrainDF[folds[[validID]],]
    trainDF <- surveyTrainDF[c(unlist(folds[trainID])),]
    
    # Trainiere k-1 Folds
    MLR_FULL <- multinom(cuadrantes ~ ., data = trainDF)
    MLR_AIC <- step(MLR_FULL, direction = 'backward', trace = 0)
    
    # Zu entfernende erklärende Variablen
    MLR_RmVar[i,] <- c(i, paste(MLR_FULL$vcoefnames[which(!(MLR_FULL$vcoefnames %in% MLR_AIC$vcoefnames))], collapse = " "))
    
    # MLR k-fache Kreuzvalidierung Vorhersage
    #MLR_yPred <- predict(MLR_AIC, newdata = validDF[-1])
    
    # MLR k-fache Kreuzvalidierung Confusion Matrix
    #MLR_ConfMat[[i]] = table(true = validDF[,1], pred = MLR_yPred)
    
    # MLR k-fache Kreuzvalidierung Genauigkeit
    #MLR_Accuracy[i,] <- c(i, sum(diag(MLR_ConfMat[[i]]))/sum(MLR_ConfMat[[i]])) 
    
}

# Evaluation MLR k-fache Kreuzvalidierung 
MLR_RmVar # zu entfernende erklärende der k Iterationen

#[, !(names(surveyDF) %in% c('sexo', 'habl_ind', 'parentesco'))]

# Finale MLR in Testdaten anwenden
MLR_Final <- multinom(cuadrantes ~ ., data = surveyDF)
MLR_Final_AIC <- step(MLR_Final, direction = 'backward', trace = 0)

#paste(MLR_Final$vcoefnames[which(!(MLR_Final$vcoefnames %in% MLR_Final_AIC$vcoefnames))], collapse = " ")

# MLR k-fache Kreuzvalidierung Vorhersage
MLR_yPred <- predict(MLR_Final, newdata = surveyTestDF[-1])

# MLR k-fache Kreuzvalidierung Confusion Matrix
MLR_ConfMat = table(true = surveyTestDF[,1], pred = MLR_yPred)

# MLR k-fache Kreuzvalidierung Genauigkeit
MLR_Accuracy <- sum(diag(MLR_ConfMat))/sum(MLR_ConfMat)

rm("i", "validID", "validDF", "trainID", "trainDF", "MLR_yPred")
```


```{r Modellierung SVM}
# install.packages("e1071")
library(e1071)

# Parameteroptimierung SVM (Liner, RBF)
linSVM_Tune <- tune.svm(cuadrantes ~ ., data = surveyTrainDF, kernel = 'linear', cost = 10^c(-3:3))
linSVM_Tune_Result <- linSVM_Tune$performances[1,2]
linSVM_Tune_Result$

RBFSVM_Tune <- tune.svm(cuadrantes ~ ., data = surveyTrainDF, 
                 gamma = 10^(c(-3:3)), 
                 cost = 10^(c(-3:3)))

# Plot tune-Ergebnisse
linSVM_tunePlot <- ggplot(tuneResult, aes(x = factor(gamma), y = factor(cost))) + geom_tile(aes(fill = Genauigkeit), color = "white") +
    geom_text(aes(label = round(Genauigkeit,3))) +
    scale_fill_gradient(low = "skyblue", high = "steelblue") +
    labs(x = "Gamma", y = "Cost")

RBFSVM_tunePlot <- ggplot(tuneResult, aes(x = factor(gamma), y = factor(cost))) + geom_tile(aes(fill = Genauigkeit), color = "white") +
    geom_text(aes(label = round(Genauigkeit,3))) +
    scale_fill_gradient(low = "skyblue", high = "steelblue") +
    labs(x = "Gamma", y = "Cost")

# Trainiere SVM (Linear, RBF)
linSVM_Modell <- svm(cuadrantes ~ .,
              data = surveyTrainDF,
              type = "C-classification",
              kernel = "linear", 
              cost = linSVM_Tune$best.parameters$cost)

RBFSVM_Modell <- svm(cuadrantes ~ .,
              data = surveyTrainDF,
              type = "C-classification",
              kernel = "radial", 
              gamma = 1, 
              cost = 1)

# Vorhersage SVM (Linear, RBF)
linSVM_yPred = predict(linSVM_Modell, newdata = surveyTestDF[-1])
RBFSVM_yPred

# SVM Confusion Matrix (Linear, RBF)
linSVM_ConfMat <- table(true = surveyTestDF[,1], pred = linSVM_yPred)
RBFSVM_ConfMat = table(true = surveyTestDF[,1], pred = RBFSVM_yPred)

# SVM Genauigkeit (Linear, Polynomial, RBF, Sigmoid)
linSVM_Accuracy <- sum(diag(linSVM_ConfMat))/sum(linSVM_ConfMat)
RBFSVM_Accuracy <- sum(diag(SVM_ConfMat))/sum(SVM_ConfMat)
```

```{r Simulation Szenario 1}
# SVM Parameteroptimierung anhand einem Paar Sample - Population
trainDF <- popList[[1]]$smp
validDF <- popList[[1]]$pop

S1_linSVM_Tune <- tune.svm(y ~ X_1 + X_2, data = trainDF, kernel = 'linear',
             gamma = 10^(c(-3:3)),
             cost = 10^(c(-3:3)))

S1_RBFSVM_Tune <- tune.svm(y ~ X_1 + X_2, data = trainDF,
             gamma = 10^(c(-3:3)),
             cost = 10^(c(-3:3)))

# MLR und SVM für jedes Paar Sample - Population
S1_MLR_Accuracy <- data.frame(Fold = integer(), Accuracy = integer())
S1_linSVM_Accuracy <- data.frame(Fold = integer(), Accuracy = integer())
S1_RBFSVM_Accuracy <- data.frame(Fold = integer(), Accuracy = integer())

S1_MLR_RmVar <- data.frame(Fold = integer(), rmVar = integer())
S1_linSVM_RmVar <- data.frame(Fold = integer(), rmVar = integer())
S1_RBFSVM_RmVar <- data.frame(Fold = integer(), rmVar = integer())

S1_MLR_ConfMat <- list()
S1_linSVM_ConfMat <- list()
S1_RBFSVM_ConfMat <- list()

for (i in c(1:simRuns)) {
    # Trainings- und Validierungsdaten
    trainDF <- popList[[i]]$smp # Sample als Trainingsdaten
    validDF <- popList[[i]]$pop # Population als Validierungsdaten
    
    # MLR Trainieren
    S1_MLR_FULL <- multinom(y ~ X_1 + X_2, data = trainDF)
    S1_MLR_AIC <- step(MLR_FULL, direction = 'backward', trace = 0)
        
    # MLR Vorhersagen
    S1_MLR_yPred <- predict(MLR_AIC, newdata = validDF[-1])
    
    # MLR Confusion Matrix
    S1_MLR_ConfMat = table(true = validDF[,1], pred = MLR_yPred)
    
    # MLR Genauigkeit
    S1_MLR_Accuracy[i,] <- c(i, sum(diag(MLR_ConfMat))/sum(MLR_ConfMat))
    
    # linSVM Trainieren
    classifierSVM <- svm(y ~ .,
                  data = trainDF,
                  type = "C-classification",
                  kernel = "radial"
                  , gamma = 0.1, 
                  cost = 10)
    
    # linSVM Vorhersage
    y_pred_SVM = predict(classifierSVM, newdata = validDF[-1])
    
    # linSVM Confusion Matrix
    confMat_SVM = table(true = validDF[,1], pred = y_pred_SVM)
    
    # linSVM Genauigkeit
    SVM_Accuracy[i,] <- c(i,sum(diag(confMat_SVM))/sum(confMat_SVM))
    
    # RBFSVM Trainieren
    classifierSVM <- svm(y ~ .,
                  data = trainDF,
                  type = "C-classification",
                  kernel = "radial"
                  , gamma = 0.1, 
                  cost = 10)
    
    # RBFSVM Vorhersage
    y_pred_SVM = predict(classifierSVM, newdata = validDF[-1])
    
    # RBFSVM Confusion Matrix
    confMat_SVM = table(true = validDF[,1], pred = y_pred_SVM)
    
    # RBFSVM Genauigkeit
    SVM_Accuracy[i,] <- c(i,sum(diag(confMat_SVM))/sum(confMat_SVM))
   
}

#warning()

boxplot(MLR_Accuracy$Accuracy, SVM_Accuracy$Accuracy)
```


```{r Szenario 2}
load("/Users/XuanSon/Desktop/complexSimData.Rdata")

# SVM Parameteroptimierung anhand einem Paar Sample - Population
trainDF <- popList[[1]]$smp
validDF <- popList[[1]]$pop

trainDF$X_5 <- as.factor(popList[[1]]$smp$X_5)
trainDF$X_6 <- as.factor(popList[[1]]$smp$X_6)
validDF$X_5 <- as.factor(popList[[1]]$pop$X_5)
validDF$X_6 <- as.factor(popList[[1]]$pop$X_6)

S2_tuneLinSVM <- tune.svm(y ~ X_1 + X_2 + X_3 + X_4 + X_5 + X_6, data = trainDF, kernel = 'linear',
             gamma = 10^(c(-3:3)),
             cost = 10^(c(-3:3)))

S2_tuneRBFSVM <- tune.svm(y ~ X_1 + X_2 + X_3 + X_4 + X_5 + X_6, data = trainDF,
             gamma = 10^(c(-3:3)),
             cost = 10^(c(-3:3)))

# MLR und SVM für jedes Paar Sample - Population
S2_MLR_Accuracy <- data.frame(Fold = integer(), Accuracy = integer())
S2_Accuracy_linSVM <- data.frame(Fold = integer(), Accuracy = integer())
S2_Accuracy_RBFSVM <- data.frame(Fold = integer(), Accuracy = integer())

for (i in c(1:simRuns)) {
    trainDF <- popList[[i]]$smp # Sample als Trainingsdaten
    validDF <- popList[[i]]$pop # Population als Validierungsdaten
    
    trainDF$X_5 <- as.factor(popList[[1]]$smp$X_5)
    trainDF$X_6 <- as.factor(popList[[1]]$smp$X_6)
    validDF$X_5 <- as.factor(popList[[1]]$pop$X_5)
    validDF$X_6 <- as.factor(popList[[1]]$pop$X_6)
    
    # MLR
    MLR_FULL <- multinom(y ~ ., data = trainDF)
    MLR_AIC <- step(MLR_FULL, direction = 'backward', trace = 0)
        
    # MLR Vorhersage
    MLR_yPred <- predict(MLR_AIC, newdata = validDF[-1])
    
    # MLR Confusion Matrix
    MLR_ConfMat = table(true = validDF[,1], pred = MLR_yPred)
    
    # MLR Genauigkeit
    MLR_Accuracy[i,] <- c(i, sum(diag(MLR_ConfMat))/sum(MLR_ConfMat))
    
    # SVM
    classifierSVM <- svm(y ~ .,
                  data = trainDF,
                  type = "C-classification",
                  kernel = "radial"
                  , gamma = 0.1, 
                  cost = 10)
    
    # SVM Vorhersage
    y_pred_SVM = predict(classifierSVM, newdata = validDF[-1])
    
    # SVM Confusion Matrix
    confMat_SVM = table(true = validDF[,1], pred = y_pred_SVM)
    
    # SVM Genauigkeit
    SVM_Accuracy[i,] <- c(i,sum(diag(confMat_SVM))/sum(confMat_SVM))
   
}

#warning()

boxplot(MLR_Accuracy$Accuracy, SVM_Accuracy$Accuracy)

```





